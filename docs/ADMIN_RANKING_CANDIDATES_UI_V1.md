# Admin ranking candidates UI

Draft UI: link from each branch, scoped history and URL-selected week detail, counted results and unpublished status. Uses the existing read-only API. Query keys include Run and Branch; no placeholder history is carried across branches. Empty, missing, invalid and error states are explicit. Names are not fabricated: the API supplies player IDs.

Validation pending: local npm ci failed on missing optional platform entries in the existing lockfile under npm 11, then the execution environment disconnected. No successful build, component test or browser verification is claimed. Backend publication and week advancement are unchanged.
